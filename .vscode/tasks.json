{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FastAPI with Hot-Reloading",
            "type": "shell",
            "command": "PYTHONPATH=\"/home/sniegras/Documents/Komp. tinklai/PHP-testing-platform/flask_backend\" uvicorn flask_backend.app.main:app --port 8000 --reload",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Run FastAPI application with hot-reloading"
        }
    ]
}
